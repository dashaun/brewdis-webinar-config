management:
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  autoconfigure:
    exclude:
    - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
    - org.springframework.cloud.aws.autoconfigure.*
  redis:
    host: brewdis-enterprise.eastus2.redisenterprise.cache.azure.net
    port: 10000
    ssl: true

stomp:
  host: dashaun-brewdis-brewdis.azuremicroservices.io
  port: 80
